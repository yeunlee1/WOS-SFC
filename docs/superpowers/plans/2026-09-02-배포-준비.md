# 배포 준비 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `배포전-리뷰수정` 브랜치를 실제 VPS 배포가 가능한 상태로 만들고 master에 병합한다.

**Architecture:** NestJS 11 서버(`server/`)와 React 웹(`web/`) 모노레포. 서버가 `web/dist`를 정적 서빙하고 docker compose(app + mysql + caddy)로 배포한다. 점검에서 확정된 결함을 테스트 먼저 작성해 고치고, 문서와 배포 구성을 정정한 뒤 로컬 MySQL과 Docker로 검증한다.

**Tech Stack:** TypeScript, NestJS 11, TypeORM 0.3, MySQL 8.4, Jest 30 + supertest, Vite/Vitest, Docker Compose, Caddy 2.

**Spec:** `docs/superpowers/specs/2026-09-02-배포-준비-design.md`

## Global Constraints

- 작업 브랜치 `배포-준비` (base `배포전-리뷰수정` db17b4b). 작업 위치 `C:\dev\wos-sfc-helper\.claude\worktrees\project-review-1adc69`.
- 커밋 제목은 `<타입>: <한국어 설명>` 형식, 설명 50자 이내.
- 신규 소스 파일 첫 줄에 한국어 한 줄 헤더 주석.
- 이미 적용된 마이그레이션 파일(000~003)은 수정하지 않는다. 새 변경은 004 파일로.
- 서버 테스트는 `npm run test:server`, 웹은 `npm run test:web`, 단일 파일은 `cd server && npx jest <경로>`.
- 스킨 브랜치는 건드리지 않는다.

---

### Task 1: NUL 문자 이스케이프

**Files:**
- Modify: `server/src/common/account-login-throttle.ts:76,100`
- Test: `server/src/common/account-login-throttle.spec.ts` (기존)

- [ ] **Step 1: 현재 상태 확인**

Run: `git diff --stat master -- server/src/common/account-login-throttle.ts`
Expected: `0 insertions(+), 0 deletions(-)` (바이너리 취급)

- [ ] **Step 2: 76·100줄의 실제 NUL 문자를 `\u0000` 이스케이프로 교체**

```ts
    const pairKey = `pair:${account}\u0000${ip}`;
```
```ts
    this.attempts.delete(`pair:${account}\u0000${ip}`);
```

- [ ] **Step 3: 테스트와 diff 확인**

Run: `cd server && npx jest src/common/account-login-throttle.spec.ts`
Expected: PASS. `git diff --stat master -- server/src/common/account-login-throttle.ts`에 줄 수가 표시된다.

- [ ] **Step 4: Commit** — `fix: 계정 로그인 제한 파일의 NUL 문자를 이스케이프로 교체`

---

### Task 2: 업로드 정적 서빙 404 수정

**Files:**
- Create: `server/src/static-serving.ts`
- Create: `server/src/static-serving.spec.ts`
- Modify: `server/src/app.module.ts:181-208`

**Interfaces:**
- Produces: `staticServingModules(options?: { webDistRoot?: string; uploadRoot?: string }): DynamicModule[]`

- [ ] **Step 1: 실패하는 테스트 작성** — `server/src/static-serving.spec.ts`

```ts
// 업로드 폴더와 web/dist 정적 서빙의 등록 순서가 /uploads/* 를 살리는지 검증한다.
import { INestApplication } from '@nestjs/common';
import { Test } from '@nestjs/testing';
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'fs';
import { tmpdir } from 'os';
import { join } from 'path';
import request from 'supertest';
import { staticServingModules } from './static-serving';

describe('정적 서빙 등록 순서', () => {
  let app: INestApplication;
  let root: string;

  beforeAll(async () => {
    root = mkdtempSync(join(tmpdir(), 'wos-static-'));
    const webDistRoot = join(root, 'web', 'dist');
    const uploadRoot = join(root, 'uploads');
    mkdirSync(webDistRoot, { recursive: true });
    mkdirSync(join(uploadRoot, 'boards'), { recursive: true });
    writeFileSync(join(webDistRoot, 'index.html'), '<html><body>WOS</body></html>');
    writeFileSync(join(uploadRoot, 'boards', 'pic.png'), Buffer.from([0x89, 0x50, 0x4e, 0x47]));

    const moduleRef = await Test.createTestingModule({
      imports: staticServingModules({ webDistRoot, uploadRoot }),
    }).compile();
    app = moduleRef.createNestApplication();
    await app.init();
  });

  afterAll(async () => {
    await app.close();
    rmSync(root, { recursive: true, force: true });
  });

  it('업로드된 파일은 200 으로 서빙된다', async () => {
    const res = await request(app.getHttpServer()).get('/uploads/boards/pic.png');
    expect(res.status).toBe(200);
    expect(res.headers['content-type']).toContain('image/png');
  });

  it('없는 업로드 파일은 404 이고 index.html 로 대체되지 않는다', async () => {
    const res = await request(app.getHttpServer()).get('/uploads/boards/missing.png');
    expect(res.status).toBe(404);
    expect(res.text).not.toContain('WOS');
  });

  it('루트는 index.html 을 준다', async () => {
    const res = await request(app.getHttpServer()).get('/');
    expect(res.status).toBe(200);
    expect(res.text).toContain('WOS');
  });

  it('web/dist 에 없는 파일은 404 다', async () => {
    const res = await request(app.getHttpServer()).get('/nope.js');
    expect(res.status).toBe(404);
  });
});
```

- [ ] **Step 2: 실패 확인** — Run: `cd server && npx jest src/static-serving.spec.ts` — Expected: FAIL (모듈 없음)

- [ ] **Step 3: 구현** — `server/src/static-serving.ts`

```ts
// 웹 빌드(web/dist)와 업로드 폴더(/uploads)를 서빙하는 ServeStaticModule 등록 목록.
import { DynamicModule } from '@nestjs/common';
import { ServeStaticModule } from '@nestjs/serve-static';
import { join } from 'path';
import { UPLOAD_ROOT } from './storage-paths';

export const WEB_DIST_ROOT = join(__dirname, '..', '..', 'web', 'dist');

/** API 라우트 접두어. web/dist 의 index.html 폴백이 이 경로들을 가로채지 않게 제외한다. */
export const STATIC_EXCLUDED_ROUTES = [
  '/auth/*path',
  '/notices/*path',
  '/rallies/*path',
  '/members/*path',
  '/boards/*path',
  '/translations/*path',
  '/users/*path',
  '/translate/*path',
  '/tts-audio/*path',
  '/admin/*path',
  '/alliance-notices/*path',
  '/me/*path',
  '/rally-groups/*path',
  '/operation-boards/*path',
  '/time',
  '/socket.io/*path',
  '/uploads/*path',
];

/**
 * 순서가 중요하다 — 업로드 서빙을 먼저 등록한다.
 *
 * web/dist 쪽은 fallthrough:false 라 자기 폴더에 없는 경로를 즉시 404 로 끝낸다.
 * exclude 목록은 index.html 폴백에만 적용되고 express.static 미들웨어에는 적용되지
 * 않으므로, web/dist 가 먼저 오면 /uploads/x.png 가 web/dist/uploads/x.png 로 해석돼
 * 업로드 이미지와 작전판 배경이 전부 404 였다. 업로드 모듈은 fallthrough 기본값(true)이라
 * 없는 파일은 web/dist 모듈로 넘어가 404 가 된다.
 */
export function staticServingModules(options: {
  webDistRoot?: string;
  uploadRoot?: string;
} = {}): DynamicModule[] {
  const webDistRoot = options.webDistRoot ?? WEB_DIST_ROOT;
  const uploadRoot = options.uploadRoot ?? UPLOAD_ROOT;
  return [
    ServeStaticModule.forRoot({
      rootPath: uploadRoot,
      serveRoot: '/uploads',
      serveStaticOptions: { index: false },
    }),
    ServeStaticModule.forRoot({
      rootPath: webDistRoot,
      exclude: STATIC_EXCLUDED_ROUTES,
      serveStaticOptions: { fallthrough: false },
    }),
  ];
}
```

`server/src/app.module.ts`: 181~208줄의 두 `ServeStaticModule.forRoot({...})` 블록을 `...staticServingModules(),` 한 줄로 바꾸고 `import { staticServingModules } from './static-serving';`를 추가한다. `ServeStaticModule`, `join`, `UPLOAD_ROOT` import가 다른 곳에서 안 쓰이면 제거한다.

- [ ] **Step 4: 통과 확인** — Run: `cd server && npx jest src/static-serving.spec.ts src/app.module.spec.ts` — Expected: PASS

- [ ] **Step 5: Commit** — `fix: 업로드 이미지 정적 서빙이 항상 404 나던 순서를 고침`

---

### Task 3: 부팅 설정 검증

**Files:**
- Create: `server/src/common/boot-config.ts`
- Create: `server/src/common/boot-config.spec.ts`
- Modify: `server/src/main.ts:61-65`, `server/src/realtime/socket-cors.options.ts` (allowedOrigin 계산부), `server/src/auth/dto/signup.dto.ts:24`

**Interfaces:**
- Produces: `resolveWebOrigin(env: NodeJS.ProcessEnv, logger: { warn(msg: string): void }): string`, `assertProductionSecrets(env: NodeJS.ProcessEnv): void`

- [ ] **Step 1: 실패하는 테스트** — `server/src/common/boot-config.spec.ts`

```ts
// 부팅 시 WEB_ORIGIN 정규화와 운영 비밀값 검증 규칙을 검증한다.
import { assertProductionSecrets, resolveWebOrigin } from './boot-config';

const silent = { warn: jest.fn() };

describe('resolveWebOrigin', () => {
  beforeEach(() => silent.warn.mockClear());

  it('끝 슬래시와 경로를 버리고 origin 만 남기며 경고를 남긴다', () => {
    expect(resolveWebOrigin({ WEB_ORIGIN: 'https://1.2.3.4.sslip.io/' }, silent)).toBe('https://1.2.3.4.sslip.io');
    expect(silent.warn).toHaveBeenCalledTimes(1);
  });

  it('포트가 있으면 유지한다', () => {
    expect(resolveWebOrigin({ WEB_ORIGIN: 'http://1.2.3.4:8080' }, silent)).toBe('http://1.2.3.4:8080');
    expect(silent.warn).not.toHaveBeenCalled();
  });

  it('production 에서 비어 있으면 던진다', () => {
    expect(() => resolveWebOrigin({ NODE_ENV: 'production' }, silent)).toThrow('WEB_ORIGIN');
  });

  it('개발에서 비어 있으면 localhost:5173 을 쓴다', () => {
    expect(resolveWebOrigin({}, silent)).toBe('http://localhost:5173');
  });

  it('URL 이 아니거나 http(s) 가 아니면 던진다', () => {
    expect(() => resolveWebOrigin({ WEB_ORIGIN: 'sfc.example.com' }, silent)).toThrow();
    expect(() => resolveWebOrigin({ WEB_ORIGIN: 'ftp://sfc.example.com' }, silent)).toThrow();
  });

  it('production 에서 localhost 면 자리표시자 경고를 남긴다', () => {
    resolveWebOrigin({ NODE_ENV: 'production', WEB_ORIGIN: 'http://localhost:5173' }, silent);
    expect(silent.warn.mock.calls.some(([m]) => String(m).includes('자리표시자'))).toBe(true);
  });
});

describe('assertProductionSecrets', () => {
  const good = { NODE_ENV: 'production', JWT_SECRET: 'x'.repeat(32), SERVER_CODE: 'code' };

  it('충분한 값이면 통과한다', () => {
    expect(() => assertProductionSecrets(good)).not.toThrow();
  });

  it('SERVER_CODE 가 비면 던진다', () => {
    expect(() => assertProductionSecrets({ ...good, SERVER_CODE: '  ' })).toThrow('SERVER_CODE');
  });

  it('JWT_SECRET 이 32자 미만이면 던진다', () => {
    expect(() => assertProductionSecrets({ ...good, JWT_SECRET: 'short' })).toThrow('32');
  });

  it('개발에서는 검사하지 않는다', () => {
    expect(() => assertProductionSecrets({ NODE_ENV: 'development' })).not.toThrow();
  });
});
```

- [ ] **Step 2: 실패 확인** — `cd server && npx jest src/common/boot-config.spec.ts` — FAIL

- [ ] **Step 3: 구현** — `server/src/common/boot-config.ts`

```ts
// 부팅 시 운영 필수 설정을 검증하고 WEB_ORIGIN 을 origin 형식으로 정규화한다.
export interface BootLogger {
  warn(message: string): void;
}

export const DEV_WEB_ORIGIN = 'http://localhost:5173';
export const MIN_JWT_SECRET_LENGTH = 32;

/**
 * WEB_ORIGIN 을 `스킴://호스트[:포트]` 로 정규화한다. 끝 슬래시와 경로는 버린다.
 * CORS 와 소켓 핸드셰이크가 이 값과 브라우저 Origin 을 글자 단위로 비교하므로,
 * 끝 슬래시 하나가 실시간 기능 전체를 조용히 끊는다.
 */
export function resolveWebOrigin(env: NodeJS.ProcessEnv, logger: BootLogger): string {
  const isProduction = env.NODE_ENV === 'production';
  const raw = (env.WEB_ORIGIN ?? '').trim();
  if (!raw) {
    if (isProduction) throw new Error('WEB_ORIGIN 환경변수가 production에서 필수입니다');
    return DEV_WEB_ORIGIN;
  }
  let url: URL;
  try {
    url = new URL(raw);
  } catch {
    throw new Error(`WEB_ORIGIN 이 URL 형식이 아닙니다: ${raw}`);
  }
  if (url.protocol !== 'http:' && url.protocol !== 'https:') {
    throw new Error(`WEB_ORIGIN 은 http 또는 https 로 시작해야 합니다: ${raw}`);
  }
  const origin = url.origin;
  if (origin !== raw) {
    logger.warn(`WEB_ORIGIN 을 ${raw} → ${origin} 으로 정규화했다 (경로·끝 슬래시 제거)`);
  }
  if (isProduction && (url.hostname === 'localhost' || url.hostname === '127.0.0.1')) {
    logger.warn(
      `WEB_ORIGIN 이 ${origin} 이다 — .env.example 의 자리표시자가 그대로 들어온 것 같다. ` +
        '브라우저가 실제로 여는 주소로 바꾸지 않으면 다른 origin 의 소켓 연결이 거부된다',
    );
  }
  return origin;
}

/** production 에서 비어 있으면 안 되는 비밀값. 부팅 전에 부르고, 걸리면 기동을 거부한다. */
export function assertProductionSecrets(env: NodeJS.ProcessEnv): void {
  if (env.NODE_ENV !== 'production') return;
  const missing = ['JWT_SECRET', 'SERVER_CODE'].filter((key) => !(env[key] ?? '').trim());
  if (missing.length > 0) {
    throw new Error(`production 에서 필수인 환경변수가 비어 있습니다: ${missing.join(', ')}`);
  }
  if ((env.JWT_SECRET as string).length < MIN_JWT_SECRET_LENGTH) {
    throw new Error(
      `JWT_SECRET 은 ${MIN_JWT_SECRET_LENGTH}자 이상이어야 합니다 (예: openssl rand -hex 32)`,
    );
  }
}
```

`server/src/main.ts` 61~65줄을 다음으로 교체하고 import를 추가한다.

```ts
  assertProductionSecrets(process.env);
  const allowedOrigin = resolveWebOrigin(process.env, new Logger('BootConfig'));
  // 소켓 CORS(socket-cors.options.ts)도 같은 정규화 값을 읽도록 env 를 덮어쓴다.
  process.env.WEB_ORIGIN = allowedOrigin;
  app.enableCors({ origin: allowedOrigin, credentials: true });
```

`server/src/realtime/socket-cors.options.ts`를 열어 `allowedOrigin`이 `process.env.WEB_ORIGIN`을 읽는 방식을 확인한다. 모듈 로드 시점에 캡처하면 위 덮어쓰기가 늦으므로, 요청 시점(함수 안)에 읽거나 `resolveWebOrigin(process.env, { warn() {} })`을 쓰도록 바꾼다.

`server/src/auth/dto/signup.dto.ts`: `serverCode`에 `@IsNotEmpty()`를 추가한다 (`class-validator`에서 import).

- [ ] **Step 4: 통과 확인** — `cd server && npx jest src/common/boot-config.spec.ts src/realtime/socket-cors.options.spec.ts src/dto-validation.spec.ts` — PASS

- [ ] **Step 5: Commit** — `feat: 부팅 시 WEB_ORIGIN 정규화와 운영 비밀값 검증 추가`

---

### Task 4: TTS 키 없음 즉시 실패

**Files:**
- Modify: `server/src/tts/tts.service.ts` (ensureFile, reportPreGenerateResult 로그), `server/src/tts/tts.controller.ts:64-68`, `server/package.json` scripts·dependencies
- Test: `server/src/tts/tts.service.spec.ts`, `server/src/tts/tts.controller.spec.ts`

**Interfaces:**
- Produces: `export class TtsUnavailableError extends Error` (tts.service.ts)

- [ ] **Step 1: 실패하는 테스트** — `tts.service.spec.ts` 끝에 추가

```ts
describe('TtsService API 키 없음', () => {
  it('키가 없고 캐시가 비면 재시도 없이 즉시 TtsUnavailableError 를 던진다', async () => {
    const cacheDir = mkdtempSync(join(tmpdir(), 'wos-tts-nokey-'));
    const service = new TtsService({
      get: (key: string) => (key === 'TTS_CACHE_DIR' ? cacheDir : ''),
    } as unknown as ConfigService);
    const started = Date.now();
    await expect(service.ensureFile('ko', '1', '1')).rejects.toBeInstanceOf(TtsUnavailableError);
    expect(Date.now() - started).toBeLessThan(300);
    rmSync(cacheDir, { recursive: true, force: true });
  });
});
```
(파일 상단 import에 `TtsUnavailableError`, `mkdtempSync`, `rmSync`, `tmpdir`, `join`이 없으면 추가한다.)

`tts.controller.spec.ts`에 추가:

```ts
  it('TTS 를 쓸 수 없으면 404 로 답하고 500 을 내지 않는다', async () => {
    service.prepareAudio.mockRejectedValue(new TtsUnavailableError('키 없음'));
    const response = makeResponse();
    await controller.serve('ko', '1', makeRequest(), response as unknown as Response);
    expect(response.status).toHaveBeenCalledWith(HttpStatus.NOT_FOUND);
  });
```
(`makeResponse`/`makeRequest` 헬퍼 이름은 기존 스펙의 것을 그대로 쓴다.)

- [ ] **Step 2: 실패 확인** — `cd server && npx jest src/tts/tts.service.spec.ts src/tts/tts.controller.spec.ts` — FAIL

- [ ] **Step 3: 구현**

`tts.service.ts` 상단(클래스 앞)에:
```ts
/** API 키가 없어 mp3 를 만들 수 없을 때. 컨트롤러는 이것을 404 로 바꾼다. */
export class TtsUnavailableError extends Error {}
```
`ensureFile`의 `if (healthy) return;` 바로 뒤에:
```ts
    if (!this.apiKey) {
      throw new TtsUnavailableError(
        `[${lang}/${key}] 캐시에 없고 GOOGLE_TTS_API_KEY 가 없어 생성할 수 없다`,
      );
    }
```
`reportPreGenerateResult`의 마지막 문구를 다음으로 교체:
```ts
      `분당 할당량 초과가 반복되면 개발 PC 에서 npm run tts:generate 로 만든 server/tts-cache 를 볼륨에 옮기거나, ` +
      `컨테이너에서 npm run tts:generate:prod 를 실행하라.`,
```
`tts.controller.ts` catch를:
```ts
    } catch (error) {
      if (error instanceof TtsUnavailableError) {
        return res.status(HttpStatus.NOT_FOUND).json({ error: 'audio unavailable' });
      }
      return res.status(HttpStatus.INTERNAL_SERVER_ERROR).json({ error: 'audio unavailable' });
    }
```
`server/package.json` scripts에 `"tts:generate:prod": "node dist/tts/scripts/tts-generate.js"` 추가, dependencies에 `"dotenv": "^16.6.1"` 추가 후 루트에서 `npm install --no-audit --no-fund` 실행해 lockfile을 갱신한다(`tts-generate.ts`가 import하는 팬텀 의존성 해소).

- [ ] **Step 4: 통과 확인** — `cd server && npx jest src/tts` — PASS

- [ ] **Step 5: Commit** — `fix: TTS 키 없이 캐시가 비면 지연 없이 404 로 응답`

---

### Task 5: 접속 목록 코얼레싱과 KST 표시

**Files:**
- Modify: `server/src/realtime/realtime.gateway.ts:45-55, 432-435`
- Test: `server/src/realtime/realtime.gateway.spec.ts`

- [ ] **Step 1: 실패하는 테스트** — 스펙 끝에 추가 (gateway/server 생성은 기존 `beforeEach`의 것을 쓴다)

```ts
describe('online:updated 코얼레싱', () => {
  beforeEach(() => jest.useFakeTimers());
  afterEach(() => jest.useRealTimers());

  it('120ms 안의 연속 호출은 한 번만 방출한다', () => {
    gateway.broadcastOnline();
    gateway.broadcastOnline();
    gateway.broadcastOnline();
    expect(server.emit).not.toHaveBeenCalledWith('online:updated', expect.anything());
    jest.advanceTimersByTime(ONLINE_COALESCE_MS);
    const calls = server.emit.mock.calls.filter(([event]) => event === 'online:updated');
    expect(calls).toHaveLength(1);
  });
});

describe('formatCreatedAt', () => {
  it('컨테이너 시간대와 무관하게 KST 로 표시한다', () => {
    expect(formatCreatedAt(new Date('2026-09-02T00:30:00Z'))).toMatch(/오전 9:30/);
  });
});
```
import에 `ONLINE_COALESCE_MS`, `formatCreatedAt`을 추가한다.

- [ ] **Step 2: 실패 확인** — `cd server && npx jest src/realtime/realtime.gateway.spec.ts` — FAIL

- [ ] **Step 3: 구현**

```ts
const KO_DATETIME_FORMAT = new Intl.DateTimeFormat('ko-KR', {
  dateStyle: 'short',
  timeStyle: 'short',
  // 컨테이너는 UTC 라 고정하지 않으면 공지·게시글 시각이 9시간 이르게 보인다.
  // DB 비교는 UTC 그대로 두고 표시만 KST 로 고정한다(앱 컨테이너에 TZ 를 넣지 말 것).
  timeZone: 'Asia/Seoul',
});

/** createdAt 표시 문자열. Date면 캐시된 포매터, 그 밖에는 그대로 문자열화. */
export function formatCreatedAt(value: unknown): string {
```
```ts
/**
 * online:updated 를 묶는 시간 창(ms). operation-boards.gateway.ts 의 PRESENCE_COALESCE_MS 와 같은 근거 —
 * 재배포 뒤 100명이 재접속하면 k번째 접속이 k명에게 k명분 목록을 보내 총량이 제곱으로 는다.
 */
export const ONLINE_COALESCE_MS = 120;
```
클래스 필드와 메서드:
```ts
  private onlineBroadcastTimer: NodeJS.Timeout | null = null;

  broadcastOnline() {
    if (this.onlineBroadcastTimer) return;
    this.onlineBroadcastTimer = setTimeout(() => {
      this.onlineBroadcastTimer = null;
      this.server.emit('online:updated', Array.from(this.onlineMap.values()));
    }, ONLINE_COALESCE_MS);
  }
```

- [ ] **Step 4: 통과 확인** — `cd server && npx jest src/realtime` — PASS. 기존 테스트가 `online:updated` 즉시 방출을 기대하면 그 테스트에 `jest.advanceTimersByTime(ONLINE_COALESCE_MS)`를 넣는다.

- [ ] **Step 5: Commit** — `perf: 접속 목록 방출을 120ms 로 묶고 시각 표시를 KST 로 고정`

---

### Task 6: 마이그레이션 러너와 메시지 인덱스

**Files:**
- Modify: `server/src/database/migrate.ts:180-189, 239-244`, `server/src/chat/message.entity.ts`
- Create: `server/src/chat/message.entity.spec.ts`

- [ ] **Step 1: 실패하는 테스트** — `server/src/chat/message.entity.spec.ts`

```ts
// messages.created_at 인덱스가 엔티티에 선언돼 dev sync 가 003 의 인덱스를 지우지 않는지 확인한다.
import { getMetadataArgsStorage } from 'typeorm';
import { Message } from './message.entity';

it('Message 엔티티는 idx_messages_created_at 인덱스를 선언한다', () => {
  const index = getMetadataArgsStorage().indices.find(
    (i) => i.target === Message && i.name === 'idx_messages_created_at',
  );
  expect(index).toBeDefined();
  expect(index?.columns).toEqual(['createdAt']);
});
```

- [ ] **Step 2: 실패 확인** — `cd server && npx jest src/chat/message.entity.spec.ts` — FAIL

- [ ] **Step 3: 구현**

`message.entity.ts`: `Index`를 import하고 `@Entity('messages')` 아래에 `@Index('idx_messages_created_at', ['createdAt'])`.

`migrate.ts` `applyMigration`의 catch 첫 줄에 `await connection.query('ROLLBACK').catch(() => {});`를 넣고, `main()`의 finally를:
```ts
  } finally {
    if (lockName) {
      try {
        await connection.query('SELECT RELEASE_LOCK(?)', [lockName]);
      } catch (releaseError) {
        console.warn(`[migrate] 잠금 해제 실패(무시): ${(releaseError as Error).message}`);
      }
    }
    await connection.end().catch(() => {});
  }
```
`server/migrations/002_dev_accounts_camelcase_rename.sql`은 수정하지 않는다(적용된 파일 규칙).

- [ ] **Step 4: 통과 확인** — `cd server && npx jest src/chat src/database` — PASS

- [ ] **Step 5: Commit** — `fix: 마이그레이션 잠금 해제가 원래 오류를 덮지 않게 하고 메시지 인덱스 선언`

---

### Task 7: 기기별 refresh 토큰

**Files:**
- Create: `server/src/auth/refresh-token.entity.ts`, `server/src/auth/refresh-tokens.service.ts`, `server/src/auth/refresh-tokens.service.spec.ts`, `server/src/auth/auth.service.spec.ts`, `server/migrations/004_refresh_tokens.sql`
- Modify: `server/src/auth/auth.service.ts`, `server/src/auth/auth.controller.ts:14-22,103-115`, `server/src/auth/auth.module.ts`, `server/src/app.module.ts` (entities), `server/src/users/users.entity.ts:42-49`, `server/src/users/users.service.ts:60-70`, `server/src/users/users.service.spec.ts:74`

**Interfaces:**
- Produces: `RefreshTokensService.issue(userId): Promise<string>`, `.rotate(userId, jti): Promise<string | null>`, `.revoke(userId, jti): Promise<void>`, `hashRefreshJti(jti): string`
- `AuthService.logout(userId: number, rawRefreshToken?: string)`

- [ ] **Step 1: 실패하는 테스트** — `server/src/auth/auth.service.spec.ts`

```ts
// 두 기기 동시 로그인·회전·부분 로그아웃이 refresh 토큰 테이블로 올바르게 동작하는지 검증한다.
import { UnauthorizedException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { FindOperator } from 'typeorm';
import { AuthService } from './auth.service';
import { RefreshToken } from './refresh-token.entity';
import { MAX_SESSIONS_PER_USER, RefreshTokensService } from './refresh-tokens.service';

const user = { id: 1, nickname: 'alice', role: 'member', allianceName: 'KOR', language: 'ko', passwordHash: '' };

/** TypeORM Repository 의 실제 호출 형태를 흉내 낸 메모리 저장소. */
function makeRepoFake() {
  const rows: RefreshToken[] = [];
  let nextId = 1;
  const matches = (row: RefreshToken, where: Record<string, unknown>) =>
    Object.entries(where).every(([key, value]) => {
      const actual = (row as unknown as Record<string, unknown>)[key];
      if (value instanceof FindOperator) {
        return value.type === 'lessThan' && (actual as Date).getTime() < (value.value as Date).getTime();
      }
      return actual === value;
    });
  return {
    rows,
    insert: jest.fn(async (r: Partial<RefreshToken>) => {
      rows.push({ id: nextId++, createdAt: new Date(), ...r } as RefreshToken);
    }),
    findOne: jest.fn(async ({ where }: { where: Record<string, unknown> }) => rows.find((r) => matches(r, where)) ?? null),
    find: jest.fn(async ({ where }: { where: Record<string, unknown> }) =>
      rows.filter((r) => matches(r, where)).sort((a, b) => b.id - a.id),
    ),
    update: jest.fn(async (id: number, patch: Partial<RefreshToken>) => {
      Object.assign(rows.find((r) => r.id === id) as RefreshToken, patch);
    }),
    delete: jest.fn(async (criteria: number[] | Record<string, unknown>) => {
      const victims = Array.isArray(criteria)
        ? rows.filter((r) => criteria.includes(r.id))
        : rows.filter((r) => matches(r, criteria));
      for (const v of victims) rows.splice(rows.indexOf(v), 1);
    }),
  };
}

describe('AuthService 기기별 refresh 토큰', () => {
  let service: AuthService;
  let repo: ReturnType<typeof makeRepoFake>;
  const usersService = {
    findByNickname: jest.fn(async () => user),
    findById: jest.fn(async () => user),
  };

  beforeEach(() => {
    repo = makeRepoFake();
    service = new AuthService(
      usersService as never,
      new JwtService({ secret: 'auth-service-spec-secret' }),
      new RefreshTokensService(repo as never),
    );
    jest.spyOn(require('bcrypt'), 'compare').mockResolvedValue(true as never);
  });

  const login = () => service.login({ nickname: 'alice', password: 'pw' });

  it('두 기기가 각자의 토큰으로 모두 갱신할 수 있다', async () => {
    const pc = await login();
    const phone = await login();
    await expect(service.refreshTokens(pc.refreshToken)).resolves.toMatchObject({ user: { id: 1 } });
    await expect(service.refreshTokens(phone.refreshToken)).resolves.toMatchObject({ user: { id: 1 } });
  });

  it('회전된 옛 토큰은 다시 쓸 수 없다', async () => {
    const pc = await login();
    await service.refreshTokens(pc.refreshToken);
    await expect(service.refreshTokens(pc.refreshToken)).rejects.toBeInstanceOf(UnauthorizedException);
  });

  it('한 기기의 로그아웃은 다른 기기를 유지한다', async () => {
    const pc = await login();
    const phone = await login();
    await service.logout(1, pc.refreshToken);
    await expect(service.refreshTokens(pc.refreshToken)).rejects.toBeInstanceOf(UnauthorizedException);
    await expect(service.refreshTokens(phone.refreshToken)).resolves.toBeDefined();
  });

  it('만료된 행은 거부한다', async () => {
    const pc = await login();
    repo.rows[0].expiresAt = new Date(Date.now() - 1000);
    await expect(service.refreshTokens(pc.refreshToken)).rejects.toBeInstanceOf(UnauthorizedException);
  });

  it('상한을 넘으면 가장 오래된 세션이 지워진다', async () => {
    const first = await login();
    for (let i = 0; i < MAX_SESSIONS_PER_USER; i += 1) await login();
    expect(repo.rows).toHaveLength(MAX_SESSIONS_PER_USER);
    await expect(service.refreshTokens(first.refreshToken)).rejects.toBeInstanceOf(UnauthorizedException);
  });

  it('refresh 쿠키 없이 로그아웃하면 아무 세션도 지우지 않는다', async () => {
    await login();
    await service.logout(1, undefined);
    expect(repo.rows).toHaveLength(1);
  });
});
```

`server/src/auth/refresh-tokens.service.spec.ts`:
```ts
// jti 해시가 결정적이고 발급 시 만료 행이 정리되는지 확인한다.
import { RefreshTokensService, hashRefreshJti } from './refresh-tokens.service';

it('같은 jti 는 같은 sha256 해시를 낸다', () => {
  expect(hashRefreshJti('abc')).toBe(hashRefreshJti('abc'));
  expect(hashRefreshJti('abc')).toHaveLength(64);
  expect(hashRefreshJti('abc')).not.toBe(hashRefreshJti('abd'));
});

it('issue 는 그 사용자의 만료 행을 먼저 지운다', async () => {
  const repo = {
    delete: jest.fn(async () => undefined),
    insert: jest.fn(async () => undefined),
    find: jest.fn(async () => []),
  };
  const service = new RefreshTokensService(repo as never);
  await service.issue(7);
  expect(repo.delete).toHaveBeenCalledWith(expect.objectContaining({ userId: 7 }));
  expect(repo.insert).toHaveBeenCalledWith(expect.objectContaining({ userId: 7 }));
});
```

- [ ] **Step 2: 실패 확인** — `cd server && npx jest src/auth` — FAIL (모듈 없음)

- [ ] **Step 3: 구현**

`server/src/auth/refresh-token.entity.ts`:
```ts
// 기기별 refresh 토큰 행. 토큰 원문 대신 jti 의 sha256 만 저장한다.
import { Column, CreateDateColumn, Entity, Index, JoinColumn, ManyToOne, PrimaryGeneratedColumn } from 'typeorm';
import { User } from '../users/users.entity';

@Entity('refresh_tokens')
@Index('idx_refresh_tokens_user_id', ['userId'])
@Index('uq_refresh_tokens_token_hash', ['tokenHash'], { unique: true })
export class RefreshToken {
  @PrimaryGeneratedColumn()
  id: number;

  @ManyToOne(() => User, { onDelete: 'CASCADE' })
  @JoinColumn({ name: 'user_id' })
  user: User;

  @Column({ name: 'user_id', type: 'int' })
  userId: number;

  @Column({ name: 'token_hash', type: 'char', length: 64 })
  tokenHash: string;

  @Column({ name: 'expires_at', type: 'datetime', precision: 6 })
  expiresAt: Date;

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date;
}
```

`server/src/auth/refresh-tokens.service.ts`:
```ts
// 기기별 refresh 토큰의 발급·회전·폐기. 계정당 세션 수를 제한하고 만료 행을 정리한다.
import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';
import { LessThan, Repository } from 'typeorm';
import { createHash, randomUUID } from 'crypto';
import { RefreshToken } from './refresh-token.entity';

export const REFRESH_TOKEN_TTL_MS = 7 * 24 * 60 * 60 * 1000;
/** 계정당 유지하는 세션(기기) 수. PC·폰·태블릿을 넉넉히 덮고, 도난 토큰의 무한 누적을 막는다. */
export const MAX_SESSIONS_PER_USER = 10;

export function hashRefreshJti(jti: string): string {
  return createHash('sha256').update(jti, 'utf8').digest('hex');
}

@Injectable()
export class RefreshTokensService {
  constructor(
    @InjectRepository(RefreshToken)
    private readonly repo: Repository<RefreshToken>,
  ) {}

  /** 새 세션을 만들고 jti 를 돌려준다. 만료 행과 상한 초과분을 함께 정리한다. */
  async issue(userId: number, now = new Date()): Promise<string> {
    await this.repo.delete({ userId, expiresAt: LessThan(now) });
    const jti = randomUUID();
    await this.repo.insert({
      userId,
      tokenHash: hashRefreshJti(jti),
      expiresAt: new Date(now.getTime() + REFRESH_TOKEN_TTL_MS),
    });
    const sessions = await this.repo.find({
      where: { userId },
      order: { id: 'DESC' },
      select: ['id'],
    });
    const surplus = sessions.slice(MAX_SESSIONS_PER_USER).map((row) => row.id);
    if (surplus.length > 0) await this.repo.delete(surplus);
    return jti;
  }

  /** 제시된 jti 의 행이 살아 있으면 새 jti 로 바꿔 돌려주고, 아니면 null. */
  async rotate(userId: number, jti: string, now = new Date()): Promise<string | null> {
    const row = await this.repo.findOne({ where: { userId, tokenHash: hashRefreshJti(jti) } });
    if (!row || row.expiresAt.getTime() <= now.getTime()) return null;
    const next = randomUUID();
    await this.repo.update(row.id, {
      tokenHash: hashRefreshJti(next),
      expiresAt: new Date(now.getTime() + REFRESH_TOKEN_TTL_MS),
    });
    return next;
  }

  /** 이 기기의 세션만 지운다. 다른 기기는 그대로다. */
  async revoke(userId: number, jti: string): Promise<void> {
    await this.repo.delete({ userId, tokenHash: hashRefreshJti(jti) });
  }
}
```

`server/src/auth/auth.service.ts` 전체를 다음 형태로 바꾼다 (signup/login 본문은 유지).
```ts
import { Injectable, UnauthorizedException, ForbiddenException } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import { UsersService } from '../users/users.service';
import { SignupDto } from './dto/signup.dto';
import { LoginDto } from './dto/login.dto';
import { RefreshTokensService } from './refresh-tokens.service';
import * as bcrypt from 'bcrypt';

interface RefreshPayload {
  sub: number;
  jti: string;
  type: string;
}

const INVALID_REFRESH = '리프레시 토큰이 유효하지 않습니다';

@Injectable()
export class AuthService {
  constructor(
    private readonly usersService: UsersService,
    private readonly jwtService: JwtService,
    private readonly sessions: RefreshTokensService,
  ) {}

  private createAccessToken(payload: { id: number; nickname: string; role: string; allianceName: string }) { /* 기존 그대로 */ }

  private signRefreshToken(userId: number, jti: string): string {
    return this.jwtService.sign({ sub: userId, jti, type: 'refresh' }, { expiresIn: '7d' });
  }

  private async createRefreshToken(userId: number): Promise<string> {
    return this.signRefreshToken(userId, await this.sessions.issue(userId));
  }

  async refreshTokens(rawRefreshToken: string) {
    let payload: RefreshPayload;
    try {
      payload = this.jwtService.verify<RefreshPayload>(rawRefreshToken);
    } catch {
      throw new UnauthorizedException(INVALID_REFRESH);
    }
    if (payload.type !== 'refresh' || typeof payload.jti !== 'string') {
      throw new UnauthorizedException(INVALID_REFRESH);
    }
    const nextJti = await this.sessions.rotate(payload.sub, payload.jti);
    if (!nextJti) throw new UnauthorizedException(INVALID_REFRESH);
    const user = await this.usersService.findById(payload.sub);
    if (!user) throw new UnauthorizedException(INVALID_REFRESH);
    return {
      accessToken: this.createAccessToken(user),
      refreshToken: this.signRefreshToken(user.id, nextJti),
      user: { id: user.id, nickname: user.nickname, role: user.role, allianceName: user.allianceName, language: user.language },
    };
  }

  /** refresh 쿠키가 있을 때만 그 기기의 세션을 지운다. 만료된 JWT 도 decode 는 되므로 폐기할 수 있다. */
  async logout(userId: number, rawRefreshToken?: string): Promise<void> {
    if (!rawRefreshToken) return;
    const payload = this.jwtService.decode(rawRefreshToken) as RefreshPayload | null;
    if (!payload || payload.type !== 'refresh' || payload.sub !== userId || typeof payload.jti !== 'string') return;
    await this.sessions.revoke(userId, payload.jti);
  }
}
```

`server/src/auth/auth.controller.ts`: refresh 쿠키 경로를 `/auth`로 바꾸고 logout이 쿠키를 넘긴다.
```ts
  private setCookies(res: any, accessToken: string, refreshToken: string) {
    res.cookie('access_token', accessToken, cookieOptions(60 * 60 * 1000));
    // /auth 전체에 보낸다 — /auth/refresh 회전과 /auth/logout 의 세션 폐기가 같은 쿠키를 본다.
    res.cookie('refresh_token', refreshToken, cookieOptions(7 * 24 * 60 * 60 * 1000, '/auth'));
  }
```
```ts
  async logout(@Req() req: any, @Res({ passthrough: true }) res: any) {
    await this.authService.logout(req.user.id, req.cookies?.refresh_token);
    res.clearCookie('access_token', { path: '/' });
    res.clearCookie('refresh_token', { path: '/auth' });
    // 경로를 넓히기 전에 발급된 쿠키도 지운다.
    res.clearCookie('refresh_token', { path: '/auth/refresh' });
    return { ok: true };
  }
```

`server/src/auth/auth.module.ts`: `TypeOrmModule.forFeature([RefreshToken])`을 imports에, `RefreshTokensService`를 providers에 추가. `server/src/app.module.ts` entities 배열에 `RefreshToken` 추가.

`server/src/users/users.entity.ts`: `refreshTokenHash` 컬럼(42~49줄) 삭제. `server/src/users/users.service.ts`: `updateRefreshTokenHash`, `findByIdWithRefreshToken` 삭제. `server/src/users/users.service.spec.ts:74` 해당 줄 삭제. `admin.service.ts` 주석의 `refreshTokenHash` 언급은 그대로 둬도 되지만 지운다.

`server/migrations/004_refresh_tokens.sql` (FK·인덱스 작성 형식은 000의 다른 테이블을 따라 맞춘다):
```sql
-- 기기별 refresh 토큰 테이블. users.refresh_token_hash(계정당 1개)를 대체한다.
-- 000 으로 만든 빈 DB 와 기존 dev DB 양쪽에서 멱등하게 동작한다.
CREATE TABLE IF NOT EXISTS `refresh_tokens` (
  `id` int NOT NULL AUTO_INCREMENT,
  `user_id` int NOT NULL,
  `token_hash` char(64) NOT NULL,
  `expires_at` datetime(6) NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  PRIMARY KEY (`id`),
  UNIQUE KEY `uq_refresh_tokens_token_hash` (`token_hash`),
  KEY `idx_refresh_tokens_user_id` (`user_id`),
  CONSTRAINT `fk_refresh_tokens_user` FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

SET @column_exists := (
  SELECT COUNT(*) FROM information_schema.COLUMNS
  WHERE TABLE_SCHEMA = DATABASE() AND TABLE_NAME = 'users' AND COLUMN_NAME = 'refresh_token_hash'
);
SET @drop_column_sql := IF(@column_exists = 1, 'ALTER TABLE `users` DROP COLUMN `refresh_token_hash`', 'DO 0');
PREPARE drop_column_stmt FROM @drop_column_sql;
EXECUTE drop_column_stmt;
DEALLOCATE PREPARE drop_column_stmt;
```

- [ ] **Step 4: 통과 확인** — `cd server && npx jest src/auth src/users src/admin src/rally-groups src/database` — PASS

- [ ] **Step 5: Commit** — `feat: refresh 토큰을 기기별로 저장해 여러 기기 동시 로그인 허용`

---

### Task 8: 배포 구성 보강

**Files:**
- Modify: `.dockerignore:24-26`, `docker-compose.yml`, `deploy/Caddyfile` 사이트 블록, `.github/workflows/test.yml`, `.env.example`

- [ ] **Step 1: .dockerignore** — `.env` / `.env.*` / `!.env.example` 세 줄을 `**/.env` / `**/.env.*` / `!**/.env.example`로.

- [ ] **Step 2: docker-compose.yml** — app.environment에 `DB_WAIT_TIMEOUT_SEC: ${DB_WAIT_TIMEOUT_SEC:-120}` 추가. 세 서비스 각각에 다음을 추가한다(값: db `1g`, app `768m`, proxy `256m`).
```yaml
    mem_limit: 768m
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
```

- [ ] **Step 3: deploy/Caddyfile** — 사이트 블록 안 `encode` 앞에:
```
	# 접근 로그를 stdout 으로 남긴다. docker compose logs proxy 로 본다.
	log {
		output stdout
		format json
	}
```

- [ ] **Step 4: test.yml** — `node-version: "20"`을 `"22"`로 (잡 `name` 값은 보호 규칙의 required check 이름이므로 바꾸지 않는다).

- [ ] **Step 5: .env.example** — Docker 절 머리에 값 규칙 한 줄(`값에 $ # 따옴표 공백을 넣지 말 것. 비밀값은 openssl rand -hex 32`), `WEB_ORIGIN` 재안내 한 줄(끝 슬래시 금지, SITE_ADDRESS 와 같은 호스트, https), `GOOGLE_TTS_API_KEY` 옆에 `첫 배포 시 캐시 볼륨이 비어 있으므로 키가 없으면 음성이 전혀 나오지 않는다`, 99줄 부근 평문 HTTP 안내를 `SITE_ADDRESS 를 비우면 로그인·화면 모두 동작하지 않는다(기동 확인용으로만)`로 고친다.

- [ ] **Step 6: 확인** — `git diff --stat` 로 5개 파일만 변경됐는지 본다. compose 문법은 Task 11에서 `docker compose config`로 검증한다.

- [ ] **Step 7: Commit** — `build: 배포 구성에 로그 회전·메모리 상한·환경변수 전달 보강`

---

### Task 9: README 정정과 운영 런북

**Files:**
- Modify: `README.md` 162~192(2-A), 208~216(3. 상태 확인) 뒤, 250~287(볼륨과 백업·갱신 배포)

- [ ] **Step 1: 2-A 절** — "방법 2 평문 HTTP" 내용을 삭제하고 다음 사실을 적는다. HTTPS 필수(secure 쿠키 + CSP upgrade-insecure-requests로 평문에서는 화면도 안 뜸), `SITE_ADDRESS`에 IP를 직접 넣으면 자체서명 인증서가 되므로 금지, sslip.io 발급이 `too many certificates` 로 실패하면 `nip.io`로 바꿔 재시도(`WEB_ORIGIN`도 함께), 도메인을 사면 이 문제가 사라짐.

- [ ] **Step 2: "### 첫 배포 체크리스트" 절 신설** (3. 상태 확인 뒤)
```markdown
### 첫 배포 체크리스트

1. **서버 시계** — `timedatectl status` 에서 `System clock synchronized: yes` 를 확인한다. 헤더의 게임 UTC 시각은 이 시계를 그대로 보여준다.
2. **최소 사양** — RAM 2GB 이상. 1GB 면 `sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile` 로 스왑을 만든 뒤 빌드한다.
3. **TTS 캐시 옮기기** — 개발 PC 의 `server/tts-cache`(mp3 796개 + `cache.meta.json`) 를 통째로 옮긴다. 메타를 빠뜨리면 첫 부팅에서 전부 삭제되고 재생성에 27분이 걸린다.
   ```bash
   # 개발 PC 에서
   tar czf tts-cache.tgz -C server tts-cache
   scp tts-cache.tgz user@서버:/tmp/
   # 서버에서 (컨테이너를 올리기 전에)
   docker volume create wos-sfc-helper_tts-cache
   docker run --rm -v wos-sfc-helper_tts-cache:/data -v /tmp:/src alpine sh -c 'tar xzf /src/tts-cache.tgz -C /data --strip-components=1 && chown -R 1000:1000 /data'
   ```
4. **첫 관리자 만들기** — 화면에서 가입한 뒤 DB 에서 역할을 올린다. developer 역할은 API 로 부여할 수 없다.
   ```bash
   docker compose exec db mysql -u root -p"$DATABASE_ROOT_PASSWORD" "$DATABASE_NAME" -e "UPDATE users SET role='developer' WHERE nickname='<닉네임>'"
   ```
   브라우저를 새로고침하면 소켓이 재접속하며 새 역할이 반영된다.
5. **프록시 단 수 확인** — 아래 "프록시 단 수 확인" 절대로 `[trust proxy 진단]` 로그를 본다.
```

- [ ] **Step 3: "### 운영 런북" 절 신설** (볼륨과 백업 앞)
```markdown
### 운영 런북

**작전 중 배포 금지.** 재배포하면 진행 중인 카운트다운·집결·라이브 작전판이 끊긴다. `docker compose logs app | grep negotiateStartedAt` 로 최근 카운트다운이 없음을 확인하고 배포한다.

**갱신 배포 (2단계).** 빌드가 실패해도 기존 컨테이너는 살아 있다.
```bash
git pull
docker compose build --pull app
docker compose up -d
docker compose ps        # app 이 healthy 인지
docker compose logs app | tail -50
```
롤백은 `git checkout <이전 태그>` 후 같은 두 명령이다. 마이그레이션은 되돌리지 않으므로 스키마를 바꾼 배포는 롤백 전에 DB 백업을 확인한다.

**마이그레이션 파일은 수정하지 않는다.** 적용된 `server/migrations/*.sql` 을 한 글자라도 고치면 체크섬 불일치로 다음 기동이 거부된다. 변경은 항상 새 번호 파일로 추가한다.

**비밀번호 초기화.** 가입에 이메일이 없어 사용자가 스스로 복구할 수 없다. 운영자가 해시를 만들어 넣는다.
```bash
docker compose exec app node -e "require('bcrypt').hash(process.argv[1],12).then(console.log)" '새비밀번호'
docker compose exec db mysql -u root -p"$DATABASE_ROOT_PASSWORD" "$DATABASE_NAME" -e "UPDATE users SET password_hash='<위 해시>' WHERE nickname='<닉네임>'"
```

**문구·음성 변경 배포.** `GOOGLE_TTS_API_KEY` 가 있어야 바뀐 mp3 가 재생성된다. 브라우저 캐시 때문에 최대 1시간 옛 음성이 섞인다.

**한 계정은 기기 10개까지.** refresh 토큰이 기기별로 저장되며 11번째 로그인부터 가장 오래된 기기가 로그아웃된다.
```

- [ ] **Step 4: 볼륨과 백업 절 보강** — DB 복원 명령과 cron 예시를 추가한다.
```markdown
복원:
```bash
docker compose exec -T db sh -c 'mysql -u root -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"' < backup-db.sql
```
매일 04:00 덤프 + 7일 보관 (crontab -e):
```
0 4 * * * cd /opt/wos-sfc-helper && docker compose exec -T db sh -c 'mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" --single-transaction "$MYSQL_DATABASE"' | gzip > /var/backups/wos-db-$(date +\%F).sql.gz && find /var/backups -name 'wos-db-*.sql.gz' -mtime +7 -delete
```
VPS 밖으로 복사하지 않으면 디스크 장애 시 전부 잃는다. `scp` 나 `rclone` 으로 매일 외부에 복사한다. 첫 배포 직후 빈 DB 에 한 번 복원해 절차가 동작하는지 확인한다.
```
기존 "갱신 배포" 절(280~287)은 런북의 2단계 절차를 가리키도록 줄인다.

- [ ] **Step 5: Commit** — `docs: 배포 절차를 실제 동작에 맞게 고치고 운영 런북 추가`

---

### Task 10: 이 PC에서 검증

- [ ] **Step 1: 전체 테스트·빌드** — 루트에서 `npm test` 와 `npm run build`. 서버·웹 테스트 수를 기록한다.
- [ ] **Step 2: 빈 DB 마이그레이션** — MySQL 서비스를 확인·기동하고 `mysql -u root -p -e "CREATE DATABASE wos_deploy_check CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci"` 후 `cd server && DATABASE_NAME=wos_deploy_check npm run migrate:dev`. 000~004 적용 로그와 `SHOW TABLES` 에 `refresh_tokens`, `schema_migrations` 가 있는지 확인한다. 두 번째 실행에서 전부 "건너뜀" 인지 확인한다.
- [ ] **Step 3: 운영 모드 기동** — `NODE_ENV=production WEB_ORIGIN=http://localhost:3001 DATABASE_NAME=wos_deploy_check TYPEORM_SYNC=false node dist/production.js` (서버 디렉토리, 빌드 후). 로그에 `[trust proxy]` 경고와 TTS 상태가 찍히는지 본다.
- [ ] **Step 4: 브라우저 확인** — `http://localhost:3001` 에서 가입(SERVER_CODE 는 server/.env 값), 로그인, 각 탭, 게시판 이미지 업로드 후 표시, 시크릿 창으로 같은 계정 두 번째 로그인 후 첫 창에서 새로고침·갱신이 유지되는지. production 이라 secure 쿠키가 http 에서 안 붙으면 이 단계는 `NODE_ENV` 없이 `TYPEORM_SYNC=false` 로 띄워 대신 확인하고 그 사실을 인계 문서에 적는다.
- [ ] **Step 5: 결과 기록** — 통과·미통과와 회피한 항목을 인계 문서 초안에 적는다.

### Task 11: Docker 설치와 컨테이너 검증

- [ ] **Step 1: WSL2** — 관리자 PowerShell에서 `wsl --install --no-distribution`. 재부팅이 필요하면 사용자에게 알리고 재부팅 후 이어간다.
- [ ] **Step 2: Docker Desktop** — `winget install --id Docker.DockerDesktop -e --accept-package-agreements --accept-source-agreements`. 실행 후 `docker version` 이 서버 버전을 보이면 완료.
- [ ] **Step 3: 루트 .env** — `.env.example` 을 복사해 `DATABASE_*`, `DATABASE_ROOT_PASSWORD`, `JWT_SECRET`(hex 32), `SERVER_CODE`, `WEB_ORIGIN=https://localhost`, `SITE_ADDRESS=localhost`, `HTTP_PORT=8080`, `HTTPS_PORT=8443` 을 채운다. `WEB_ORIGIN` 은 `https://localhost:8443`.
- [ ] **Step 4: 빌드·기동** — `docker compose config` → `docker compose build` → `docker compose up -d` → `docker compose ps` 에서 세 서비스 healthy/running, `docker compose logs app` 에 마이그레이션 5개 적용과 `Server running`.
- [ ] **Step 5: 접속** — `https://localhost:8443` (Caddy 내부 인증서 경고는 통과) 에서 가입·로그인·탭·소켓 연결(접속자 목록) 확인. `curl -sk https://localhost:8443/time`.
- [ ] **Step 6: 정리** — `docker compose down -v` 로 로컬 볼륨 삭제. 루트 `.env` 는 gitignore 대상임을 `git status` 로 확인.

### Task 12: PR·병합·인수인계

- [ ] **Step 1: 푸시·PR** — `git push -u origin 배포-준비` 후 `gh pr create --base master` 제목·본문 한글. CI 2개 잡과 CodeQL 통과 확인.
- [ ] **Step 2: 병합** — `gh pr merge --squash`. master 를 pull 해 태그 `배포준비-2026-09-02` 를 만든다.
- [ ] **Step 3: 인수인계 문서** — `인수인계` 스킬로 `C:\Users\ADMIN\Desktop\클로드 출력물\2026-09-02-WOS-배포준비-인수인계.md` 를 쓴다. 검증 결과, 보증하지 못한 것, VPS 배포 시 실행할 명령 순서, 남은 결정(호스팅·스킨·도메인)을 담는다.
