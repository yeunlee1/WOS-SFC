# 동화 버전 UI 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/story` 에서 뜨는 수채화 그림책 결의 두 번째 UI 를 만들고, 기존 `/` 앱은 그대로 둔다.

**Architecture:** `main.jsx` 가 경로로 `App`/`StoryApp` 을 lazy 로드한다. StoryApp 은 App 과 같은 부트스트랩(세션 복원·소켓·시계 동기화)을 갖고, 새 껍데기 안에 기존 기능 컴포넌트를 끼워 넣어 `story.css` 로 새로 입힌다.

**Tech Stack:** React 18, Vite 8, Zustand, Vitest 4 + Testing Library, Playwright(MCP) 스크린샷.

**Spec:** `docs/superpowers/specs/2026-09-02-동화-버전-design.md`

## Global Constraints

- 브랜치 `동화-버전`(base master 623baf9). 작업 위치 `C:\dev\wos-sfc-helper\.claude\worktrees\project-review-1adc69`.
- 기존 컴포넌트 파일은 수정하지 않는다. 예외는 `main.jsx`(진입 분기)와 `App.jsx`(style.css import 이동) 두 곳.
- 새 소스 파일 첫 줄에 한국어 헤더 주석. 이모지 아이콘 대신 `icons.jsx` 의 SVG.
- 모든 화면은 768px·480px 에서 1열로 동작해야 한다. 고정 px 너비 금지.
- 테스트 명령 — `cd web && npx vitest run <경로>`. 전체는 루트 `npm run test:web`.
- 커밋 제목 `<타입>: <한국어 설명>`.

---

### Task 1: 진입 분기

**Files:**
- Create: `web/src/entry.js`, `web/src/__tests__/entry.spec.js`
- Modify: `web/src/main.jsx`, `web/src/App.jsx:1` (import 추가)

**Interfaces:**
- Produces: `resolveEntry(pathname: string): 'story' | 'main'`

- [ ] **Step 1: 실패하는 테스트** — `web/src/__tests__/entry.spec.js`

```js
// 경로에 따라 기존 앱과 동화 버전 중 무엇을 띄울지 정하는 규칙을 검증한다.
import { describe, it, expect } from 'vitest';
import { resolveEntry } from '../entry';

describe('resolveEntry', () => {
  it('/story 와 그 하위 경로는 동화 버전이다', () => {
    expect(resolveEntry('/story')).toBe('story');
    expect(resolveEntry('/story/')).toBe('story');
    expect(resolveEntry('/story/battle')).toBe('story');
  });
  it('그 밖의 경로는 기존 앱이다', () => {
    expect(resolveEntry('/')).toBe('main');
    expect(resolveEntry('/storybook')).toBe('main');
    expect(resolveEntry('/battle')).toBe('main');
  });
});
```

- [ ] **Step 2: 실패 확인** — `cd web && npx vitest run src/__tests__/entry.spec.js` — FAIL

- [ ] **Step 3: 구현**

`web/src/entry.js`
```js
// 경로에 따라 기존 앱(main)과 동화 버전(story) 중 무엇을 띄울지 정한다.
export function resolveEntry(pathname) {
  return pathname === '/story' || pathname.startsWith('/story/') ? 'story' : 'main';
}
```

`web/src/main.jsx`
```jsx
import React, { Suspense } from 'react';
import ReactDOM from 'react-dom/client';
import { I18nProvider } from './i18n';
import { resolveEntry } from './entry';

// 두 앱은 서로의 CSS 를 로드하지 않는다 — 각 앱이 자기 스타일을 import 하고 lazy 로 청크가 갈린다.
const Root =
  resolveEntry(window.location.pathname) === 'story'
    ? React.lazy(() => import('./story/StoryApp'))
    : React.lazy(() => import('./App'));

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <I18nProvider>
      <Suspense fallback={null}>
        <Root />
      </Suspense>
    </I18nProvider>
  </React.StrictMode>
);
```

`web/src/App.jsx` 첫 import 뒤에 `import '../style.css';` 추가.

- [ ] **Step 4: 통과 확인** — entry.spec PASS. `npm run test:web` 전체 PASS(226건 유지).
- [ ] **Step 5: Commit** — `feat: 경로로 기존 앱과 동화 버전을 나눠 띄우는 진입 분기 추가`

---

### Task 2: 스타일 기반과 아이콘

**Files:**
- Create: `web/src/story/story.css`, `web/src/story/icons.jsx`

- [ ] **Step 1: story.css 골격** — 순서대로 담는다.
  1. Google Fonts `@import`(Gowun Batang 400/700, Gowun Dodum, Gaegu 400/700).
  2. `:root` 토큰(spec 의 색·글꼴), `body.story-body { margin:0; background:#ece4d6; }`.
  3. `.story-root` — `min-height:100dvh`, 종이색, 본문 글꼴, `position:relative; overflow-x:hidden`.
  4. `.story-paper`(feTurbulence 오버레이, `pointer-events:none`), `.story-wash`(blur 46px, multiply).
  5. 원시 — `.story-card`(손그림 테두리), `.btn/.btn-primary/.btn-danger/.btn-ghost/.btn-sm`, `.input/.textarea`, `.section/.section-title/.section-desc/.section-header`, `.empty-message`, `.chip`, `.serif`, `.hand`.
  6. 껍데기 — `.story-shell`(grid: rail 84px | 본문 | 260px 패널), `.story-rail`, `.story-rail-item(.active)`, `.story-topbar`, `.story-frame`, `.story-online`.
  7. 반응형 — `@media (max-width: 768px)`: 레일이 하단 고정 바(`position:fixed; bottom:0`), 패널 숨김(토글로 오버레이), 본문 1열. `@media (max-width: 480px)`: 상단바 항목 축소.
  재사용 컴포넌트 클래스(`cd-*`, `rally-*`, `personal-panel`, `timeline-*`, `post-card`, `notice-row`, `board-*`, `chat-tab-*`, `online-*`, `operation-*`, `admin-*`)는 Task 5~8 에서 각 화면을 만들며 같은 파일 아래쪽에 절을 나눠 추가한다.

- [ ] **Step 2: icons.jsx** — `Icon({ name, size = 20 })` 하나. name: `book`, `sword`, `map`, `bookmark`, `chat`, `shield`, `clock`, `volume`, `mute`, `globe`, `logout`, `play`, `stop`, `plus`, `send`, `image`, `users`. 시안의 SVG 경로를 그대로 옮긴다(stroke 1.8, round).

- [ ] **Step 3: 확인** — `npx vite build` 가 CSS 문법 오류 없이 통과. Commit — `feat: 동화 버전 스타일 토큰·원시 요소·아이콘 추가`

---

### Task 3: 입구 (StoryEntrance)

**Files:**
- Create: `web/src/story/StoryEntrance.jsx`, `web/src/story/__tests__/StoryEntrance.spec.jsx`

**Interfaces:**
- Produces: `<StoryEntrance />` — 로그인 성공 시 `useStore.setUser(user)` 와 `changeLang(user.language)` 를 호출한다(AuthModal 과 같은 계약).

- [ ] **Step 1: 실패하는 테스트**

```jsx
// 동화 입구의 로그인·가입이 기존 인증 계약(api → setUser → changeLang)을 지키는지 검증한다.
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen, cleanup, fireEvent, waitFor } from '@testing-library/react';
import { I18nProvider } from '../../i18n';
import { useStore } from '../../store';
import StoryEntrance from '../StoryEntrance';

const api = vi.hoisted(() => ({ login: vi.fn(), signup: vi.fn() }));
vi.mock('../../api', () => ({ api }));
vi.mock('../../components/Battle/rallyGroupPlayer', () => ({ warmupRallyAudio: vi.fn(async () => {}) }));

function renderEntrance() {
  return render(<I18nProvider><StoryEntrance /></I18nProvider>);
}

describe('StoryEntrance', () => {
  beforeEach(() => { cleanup(); useStore.setState({ user: null }); api.login.mockReset(); api.signup.mockReset(); });

  it('빈 값으로 입장하면 안내를 보여주고 API 를 부르지 않는다', async () => {
    renderEntrance();
    fireEvent.click(screen.getByRole('button', { name: '입장' }));
    expect(await screen.findByText('닉네임과 비밀번호를 입력하세요')).toBeInTheDocument();
    expect(api.login).not.toHaveBeenCalled();
  });

  it('로그인 성공 시 사용자를 저장한다', async () => {
    api.login.mockResolvedValue({ user: { id: 1, nickname: 'alice', language: 'ko', role: 'member', allianceName: 'KOR' } });
    renderEntrance();
    fireEvent.change(screen.getByLabelText('닉네임'), { target: { value: 'alice' } });
    fireEvent.change(screen.getByLabelText('비밀번호'), { target: { value: 'secret1' } });
    fireEvent.click(screen.getByRole('button', { name: '입장' }));
    await waitFor(() => expect(useStore.getState().user?.nickname).toBe('alice'));
    expect(api.login).toHaveBeenCalledWith({ nickname: 'alice', password: 'secret1' });
  });

  it('가입 페이지에서 닉네임 규칙을 어기면 거부한다', async () => {
    renderEntrance();
    fireEvent.click(screen.getByRole('button', { name: /가입 코드로 이야기에 합류/ }));
    fireEvent.change(screen.getByLabelText('닉네임'), { target: { value: 'a b' } });
    fireEvent.change(screen.getByLabelText('비밀번호'), { target: { value: 'secret1' } });
    fireEvent.click(screen.getByRole('button', { name: 'KOR' }));
    fireEvent.change(screen.getByLabelText('가입 코드'), { target: { value: 'code' } });
    fireEvent.click(screen.getByRole('button', { name: '합류하기' }));
    expect(await screen.findByText(/닉네임은 한글 또는 영문\/숫자만/)).toBeInTheDocument();
    expect(api.signup).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패 확인** — FAIL (모듈 없음)
- [ ] **Step 3: 구현** — 시안 Main 화면 구조. 왼쪽 페이지: "제 1장", `WOS · SFC`, "눈 내리는 왕국의 작전 일지", 한 문단, 일러스트 SVG. 오른쪽 페이지: `mode === 'login'` 이면 닉네임·비밀번호·[입장]과 "처음 오셨나요? 가입 코드로 이야기에 합류하기" 버튼, `signup` 이면 닉네임·비밀번호·연맹 칩(버튼 그룹, `aria-pressed`)·언어 select·가입 코드·[합류하기]·"이미 계정이 있어요 → 입장으로" 버튼. 핸들러와 검증은 AuthModal.jsx 35~96줄을 그대로 옮긴다(`NICKNAME_REGEX`, 6자 규칙, `initUser`). 입력마다 `<label htmlFor>`. 배경 물감·언덕·성·눈송이는 시안 SVG 를 컴포넌트 안에 둔다. `@media (max-width: 768px)` 에서 책은 한 페이지(오른쪽 폼)만 보이고 브랜드는 위에 작게.
- [ ] **Step 4: 통과 확인** — 3건 PASS. Commit — `feat: 동화 버전 입구 화면(펼쳐진 책 로그인·가입) 추가`

---

### Task 4: 껍데기와 부트스트랩 (StoryApp · StoryShell · StoryTopbar · StoryRail)

**Files:**
- Create: `web/src/story/StoryApp.jsx`, `StoryShell.jsx`, `StoryTopbar.jsx`, `StoryRail.jsx`, `web/src/story/__tests__/StoryShell.spec.jsx`

**Interfaces:**
- `StoryShell({ activeTab, onTabChange, children })` — 레일·상단바·프레임·접속 패널을 그리고 `children` 을 프레임에 넣는다.
- `StoryRail({ activeTab, onTabChange })` — 탭 목록 `battle, operation, community, chat` + `admin`(user.role === 'developer').
- `StoryTopbar({ activeTab })` — Header.jsx 의 시계·배지·TTS 제어·언어를 같은 스토어 필드로 구현, 사용자 칩 클릭 시 로그아웃 메뉴.

- [ ] **Step 1: 실패하는 테스트** — `StoryShell.spec.jsx`

```jsx
// 동화 껍데기가 탭을 전환하고, 관리자 탭은 developer 에게만 보이며, 로그아웃이 세션을 정리하는지 검증한다.
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render, screen, cleanup, fireEvent, waitFor } from '@testing-library/react';
import { I18nProvider } from '../../i18n';
import { useStore } from '../../store';
import StoryShell from '../StoryShell';

const api = vi.hoisted(() => ({ logout: vi.fn(async () => ({ ok: true })) }));
vi.mock('../../api', () => ({ api, getSocket: () => null, disconnectSocket: vi.fn() }));
vi.mock('../../components/Battle/tts', () => ({ speak: vi.fn(), stopAllTts: vi.fn() }));

const member = { id: 1, nickname: 'alice', role: 'member', allianceName: 'KOR', language: 'ko' };

function renderShell(props = {}) {
  const onTabChange = vi.fn();
  render(
    <I18nProvider>
      <StoryShell activeTab="battle" onTabChange={onTabChange} {...props}>
        <div>본문</div>
      </StoryShell>
    </I18nProvider>,
  );
  return { onTabChange };
}

describe('StoryShell', () => {
  beforeEach(() => {
    cleanup();
    useStore.setState({ user: member, onlineUsers: [], timeSyncState: 'unsynced', timeSyncRtt: 0, timeOffset: 0, ttsMuted: false, ttsVolume: 0.3 });
  });

  it('탭 버튼을 누르면 onTabChange 를 부른다', () => {
    const { onTabChange } = renderShell();
    fireEvent.click(screen.getByRole('button', { name: '커뮤니티' }));
    expect(onTabChange).toHaveBeenCalledWith('community');
  });

  it('관리자 탭은 developer 에게만 보인다', () => {
    renderShell();
    expect(screen.queryByRole('button', { name: '관리자' })).toBeNull();
    cleanup();
    useStore.setState({ user: { ...member, role: 'developer' } });
    renderShell();
    expect(screen.getByRole('button', { name: '관리자' })).toBeInTheDocument();
  });

  it('로그아웃하면 API 를 부르고 사용자를 비운다', async () => {
    renderShell();
    fireEvent.click(screen.getByRole('button', { name: /alice/ }));
    fireEvent.click(screen.getByRole('button', { name: '로그아웃' }));
    await waitFor(() => expect(useStore.getState().user).toBeNull());
    expect(api.logout).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: 실패 확인** — FAIL
- [ ] **Step 3: 구현**
  - `StoryApp.jsx`: App.jsx 82~148줄의 effect(세션 복원 `api.getMe` → `setUser` → `changeLang` → `warmupRallyAudio`, `visibilitychange` 재동기화, `auth:expired`, `startup/shutdown`, 소켓 connect 재동기화)와 `useSocket(user, lang)`, `useReadyProbe(user)` 를 그대로 둔다. `document.body.classList.add('story-body')`, `#app-splash` 제거. `hydrating` 동안 null. `user` 없으면 `<StoryEntrance />`, 있으면 `<StoryShell activeTab onTabChange>{탭 컴포넌트}</StoryShell>`. 탭 상태는 `useState('battle')`.
  - `StoryRail.jsx`: 시안의 책갈피 레일. 탭마다 `<button aria-label={label} aria-current>` + `Icon` + 라벨. 모바일에서는 CSS 로 하단 바.
  - `StoryTopbar.jsx`: Header.jsx 80~91줄의 UTC 시계 effect, `_syncBadge` 와 같은 상태 문구(동기화됨 ±Nms / 동기화 중 / 동기화 실패 / 미동기화)를 텍스트로, 음성 켬/끔 버튼(`ttsMuted`, `stopAllTts`), 볼륨 range, 테스트 버튼(`speak('start', lang, { force: true })`), 언어 select(`SUPPORTED_LANGS`), 사용자 칩(`nickname · allianceName`, 클릭 시 메뉴: 역할 표시, 로그아웃). 로그아웃 — `api.logout()` → `disconnectSocket()` → `clearUser()`.
  - `StoryShell.jsx`: `.story-shell` grid 에 `StoryRail`, 본문(`StoryTopbar` + `.story-frame` children), 오른쪽 `OnlinePanel isOpen` (채팅 탭에서는 숨김 — ChatTab 이 자체 사이드바를 가짐).
- [ ] **Step 4: 통과 확인** — 3건 PASS + 전체 web 테스트 PASS. Commit — `feat: 동화 버전 껍데기(레일·상단바·프레임)와 부트스트랩 추가`

---

### Task 5: 전투현황

**Files:**
- Create: `web/src/story/StoryBattle.jsx`
- Modify: `web/src/story/story.css` (전투 절)

- [ ] **Step 1: 구성** — `.story-battle` grid 2열(768px 이하 1열). 왼쪽: `.story-card` 안에 `<Countdown />`, 아래 `.story-card` 안에 "수비 카운트" `<CountdownDots />` 와 "공격 카운트" `<RallyDots />`. 오른쪽: `.story-card` 안 `<PersonalPanel />`, `.story-card.story-card--butter` 안 `<RallyGroupPanel />`.
- [ ] **Step 2: CSS** — `.cd-section/.cd-hero/.cd-hero-head/.cd-hero-tag(.active,.danger)/.cd-mega/.cd-controls/.cd-presets/.cd-preset-btn(.selected)/.cd-input-row/.cd-input/.cd-btn-start/.cd-btn-stop/.cd-key-hint kbd/.cd-viewer-msg/.cd-error-msg`, `.battle-viz-mobile`(데스크톱 숨김), `.timeline*`, `.personal-panel/.my-rally-*/.march-*`, `.rally-group-panel/.rally-group-list/.rally-member-*/.rally-btn(--primary,--danger,--warn)/.rally-error/.rally-empty/.rally-group-countdown/.rally-timeline`. 숫자 `.cd-mega` 는 `clamp(96px, 14vw, 160px)`, 색 `#2f2a3f`, `font-variant-numeric: tabular-nums`.
- [ ] **Step 3: 확인** — Task 9 의 스크린샷에서 시안 Battle 과 대조. Commit — `feat: 동화 버전 전투현황 화면 구성과 스타일`

### Task 6: 커뮤니티

- [ ] `StoryCommunity.jsx` — CommunityTab.jsx 의 상태 로직을 옮기되 연맹 탭에서는 `<AllianceNoticeboard alliance />` 와 `<Board alliance />` 를 2열로 같이 렌더(서브탭 없음). 탭은 `.story-tab(.active)` 색인 모양, 연맹별 파스텔 배경.
- [ ] CSS — `.story-community-frame`, `.section-header`, `.compose-card`, `.post-card/.post-icon/.post-meta/.post-title`, `.notice-row*`, `.notice-detail-*`, `.notice-original*`, `.translated`, `.board-*`, `.image-*`, `.emoji-*`.
- [ ] Commit — `feat: 동화 버전 커뮤니티 화면(색인 탭·공지와 게시판 2열)`

### Task 7: 채팅

- [ ] StoryApp 에서 `chat` 탭은 `<ChatTab />` 를 `.story-frame.story-frame--chat` 에 넣는다(접속 패널 숨김).
- [ ] CSS — `.chat-tab-layout/.chat-tab-main/.chat-tab-topbar/.chat-tab-messages/.chat-tab-msg(.me)/.chat-tab-msg-avatar/.chat-tab-msg-head/.chat-tab-msg-text/.chat-tab-toggle-original/.chat-tab-system-msg/.chat-tab-input-row/.chat-tab-input/.chat-tab-send-btn/.chat-tab-sidebar*/.chat-autotranslate-toggle/.chat-send-error`. 말풍선은 시안대로 손그림 테두리, 내 메시지는 살구색.
- [ ] Commit — `feat: 동화 버전 채팅 화면 스타일`

### Task 8: 작전판·관리자

- [ ] `operation` 탭 `<OperationBoardTab />`, `admin` 탭 `<AdminTab />` 를 프레임에 넣고 `.operation-*`, `.admin-*` 최소 스타일(도구막대·버튼·표). 캔버스 자체는 손대지 않는다.
- [ ] Commit — `feat: 동화 버전에 작전판·관리자 탭 연결`

### Task 9: 검증

- [ ] `npm run test:web` 전체 PASS(기존 226 + 신규). `npm run build --workspace web` — `dist/assets` 에 `StoryApp-*.js` 와 `StoryApp-*.css` 가 따로 있고 `index.html` 은 둘 다 링크하지 않는다(lazy).
- [ ] 로컬 실행 — 검증용 mysqld(3307, 스크래치패드 데이터) 기동, 서버 `npm run start:dev` 를 `PORT=3011 DATABASE_PORT=3307 DATABASE_USER=root DATABASE_PASSWORD= DATABASE_NAME=wos_deploy_check SERVER_CODE=deploycheck WEB_ORIGIN=http://localhost:5174` 로, 웹 `VITE_API_TARGET=http://localhost:3011 npx vite --port 5174`.
- [ ] Playwright — `http://localhost:5174/story` 입구 스크린샷(1440·390), 가입(닉네임 storytester), 전투현황·커뮤니티·채팅·작전판 스크린샷, `http://localhost:5174/` 가 기존 화면인지 확인. 어긋난 부분은 CSS 로 고쳐 재촬영.
- [ ] Commit — `test: 동화 버전 화면 검증 결과 반영` (수정이 있을 때만)

### Task 10: PR

- [ ] `git push -u origin 동화-버전`, `gh pr create --base master` 제목 `동화 버전 UI 추가 (/story)`, 본문에 스크린샷 경로와 검증 결과. CI 통과 후 squash 병합.
